//Code of binary search
//elememts should be in a monotonous function(ascending / descending / sorted)
/*
Algorithm:

Find mid
compare mid and key
    if = then return index
    else(i.e !=) then decide which part to proceed further
repeat until found else return -1.

*/

/*
mid = ( start - end ) / 2;

but more optimized formula for mid is:

mid = start + ( ( end - start) / 2 );

*/
#include<iostream>

using namespace std;

int main(){

    cout<<"H"<<endl;
    return 0;
}

